Elder Fenn. Authored posed turtle (aa0b9c1). One mesh. Face, shell, cane.

  fenn.fbx            unsplit body. Do not run drop_character.py --bind segmented on this.
  fenn-albedo.png     1024 base color (URP Lit). Required for paint.
  Resources/Art/Characters/fenn/   player copies (fbx + albedo)

bind=rigid. Python vertex-split shredded the face into a black interior.
Quality path is painted weights (bind=skinned) on a T/A-pose, not another split.
Idle/pose clip files stay for the catalog; they must not drive this statue.
Humans pass look. Spec: docs/character-package.md


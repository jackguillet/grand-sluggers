Elder Fenn. Authored posed turtle. One mesh. Face, shell, cane.

  fenn.fbx            unsplit body + fitted Generic armature + hard 1.0 groups
  fenn-idle.fbx / fenn-pose.fbx   takes on THIS armature (pose = check-swing)
  fenn-albedo.png     1024 base color (URP Lit)
  Resources/Art/Characters/fenn/   player copies

bind=skinned. Hard groups: shell/head/plastron = torso; limbs by centroid.
Do not --bind segmented (that shredded the face). Do not heat-weight.
Clay rest must show a face; clay 90° arm may stretch the limb — pose take is ~38°.
Painted weights are the quality fill. Humans pass look.
Spec: docs/character-package.md


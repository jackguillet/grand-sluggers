# At-bat input evidence (#534)

`2aea26c-passed.json` records the actual Controls → TickSet/TickFlight path in GUI Unity 6000.5.9f1 using temporary virtual gamepads and keyboard. All cases passed, including release during windup, seat-two isolation from keyboard release, camera-relative left/right, and cursor independence from pitch curve. Physical-controller and human gameplay acceptance remain open.

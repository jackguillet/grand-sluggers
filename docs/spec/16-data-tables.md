# 16. Data tables (rails)

Presentation stays in `data/feel/`. Rules live in `data/rules/` (✅ P0), loaded by `RulesTable` into `ContentCatalog.Rules` the way `FeelTable` is loaded: the JSON is the only source of a rule number. The tables hold no C# defaults, and `ContentDataValidator` (so `cli art` and every `ContentCatalog.Load`) refuses a missing file, a missing field, an unknown field, or a value outside its declared range (`[Positive]`, `[Chance]`, `[Signed]`); only an `[Optional]` row may be absent.
A table changed in code is a `with` copy of a loaded one. Every helper that reads a number takes the table as a required argument (`Match.Rules`, `ContentCatalog.Rules`), so no helper can fall back to another table.
Only entry points (the CLI, the Unity bootstrap, tools, tests) and the one shared diamond (`Diamond`, `ParkDiamond`'s dress, `ParkBoundary.Default`) read the process table found from the data root, and a process that finds no readable root stops. **Any new rule number is a field here with a validator, never a C# literal.** The infield joined them in `infield.json`:
every park shares one diamond, so it is one global set loaded once and read-only thereafter — nothing may change it mid-run, because tests execute in parallel. The ground that dresses that diamond joined it in the same file:
the drawn grass diamond (`innerHalfFt`) and the back arc of the dirt (`backArcFt`) are measured from the bags and the rubber, so they travel with them — a deliberate exception to "presentation stays in `data/feel/`", because a picture measured from geometry is not free to stay behind when the geometry moves. The seven fielder starts followed in `fielders.json`, for the same reason and on the same terms:
every park shares them, so they are one global set loaded once, and a trial root stands a different defence without the shipped table being touched. The rest of the structural geometry stays constant: the plate frame (`StrikeZoneGeometry.Reference`, `PlateScaleX/Y`; each batter's zone scales it vertically, §4.4) and the 45° foul line the wall and stands meshes share.
The rest of the dress stays in feet as `const` on `ParkDiamond` — path width, the bag pads, the home pad, the mound table, the warning track and the foul apron are bodies and equipment, and a smaller diamond does not make a fielder smaller. To play a trial infield, point a whole process at a trial overlay with `GRAND_SLUGGERS_TRIAL` (below) and diff the two runs; the comparison is process-level, never a table swapped inside one run.
`GRAND_SLUGGERS_DATA` points at a whole second data root and stays available for the rare case that wants one.

## Randomness

A match draws from one seed through named streams (`MatchStreams`), split by name with `SimRandom.Stream`: `pitch-ai` (the CPU pitcher, the pickoff read, the CPU catcher's release), `bat-ai` (the CPU swing, box, bunt square and steal plan), `contact` (the at-bat resolver), `handling` (the fielding resolve, drops, bobbles, deflections and throw quality) and `hazard` (what a park hazard does, FD-08). A draw on one stream never moves another.
The generator is the sim's own (`SimRandom`, xoshiro256** seeded through SplitMix64), so a seed plays the same game on .NET and on Mono. The sim constructs no `System.Random` (S-92).

## Trial overlays

A candidate profile is authored as an **overlay**, not as a second `data/` tree: a folder under `trials/` that carries only the files it overrides and resolves everything else against the shipped root. `GRAND_SLUGGERS_TRIAL=trials/<name>` lays one over the data root for a whole process — absolute, or relative to the folder `data/` sits in — and every `cli` run prints the root and the overlay it read on stderr, so a trace can be attributed after the fact.

A second full tree would duplicate characters, bats, gloves, chemistry, abilities, art and feel, none of which a rules trial changes; the first edit to a character would make the trial and the control differ on something the trial never meant to own, and the comparison the trial exists to make would be quietly worthless. So:

- **Whole files, never fields.** A trial's copy must carry every field the shipped file carries, and a partial one is refused by name. Field-level merging makes a table that is half one profile and half another, and a field the trial does not name would play the shipped value while provenance claims the whole table. Array rows are the trial's own business: a park may carry a different number of hazards.
- **The tree is the declaration.** A file is overridden because the overlay carries it. A manifest would be a second place to keep in sync, and a file that fell out of it would silently run the control.
- **Override, never add.** A path the shipped root does not have stops the run, for the same reason `GRAND_SLUGGERS_DATA` refuses a root with no `characters/`. That includes a name differing only in case: the declaration is built by enumerating the shipped root, so a spelling the shipped root does not use is a stray file on any filesystem rather than an override that works on a laptop and vanishes in CI. `README.md` at the top of an overlay is the one exception, because a trial that cannot explain itself is not evidence either.
- **Every run stops rather than falls back.** Tables that cannot be read throw, whether or not either variable is set: there is no code table, and `Diamond` reads the process table, so any substitute would put the bags where the data never said.
- **Inert unless named.** A checked-in trial does not touch a run that did not ask for it, and the overlay rides only on the root the environment resolves — a root a caller passes in (a test fixture, Unity) is exactly that root.

A saved `--trace` carries the overlay it ran under and omits the field entirely for a control run, so a trace file attributes itself without the operator having kept the stderr line.

Trial anchors are not shipping defaults. Flipping one is its own step, after the trial has been measured.

The overlays in the tree:

| Overlay | What it proposes |
| --- | --- |
| — | None today. |

**Kept dormant.** With no overlay in the tree, the machinery stays so the next trial is a folder, not new code: `DataRoot`, `GRAND_SLUGGERS_TRIAL` and `GRAND_SLUGGERS_DATA`, the Unity window's `DataProfile`, `tools/local-player.py --trial`, and `TrialOverlayTests`, which builds its overlays in temporary folders. Tests that read the shipped root are named for what they test, not for the trial that first proposed it.

**Also retired:** `trials/fatigue`, `trials/stars`, `trials/bunt` and `trials/cpu-read`. Each was promoted into `data/` and its folder deleted.

**Retired:** `trials/c80`. The compact profile — the 80-ft field, the heavier ball, and the reads, clocks and throws derived to match them — became the shipped game when Jack decided to promote it (3e, the ordinary loop only; §0.2 holds its contract). Every value it carried is in `data/`, and the folder was deleted.

**Retired:** `trials/pitch5` (P1-d, P1-g, P2-b, P2-c). Both its trials, the duel and the stick, live in `data/rules/`, so the folder is gone. Reports: [`docs/research/promote-duel-trial.md`](../research/promote-duel-trial.md), [`docs/research/promote-stick.md`](../research/promote-stick.md).

**Park files (`data/parks/*.json`) are held to that standard.** ✅: a key a park file does not declare is an error that names the file and the key, in the park object and in a hazard row alike (`ContentDataValidator.UnknownKeys`, the shape `RulesValidation.UnknownFields` uses), on the shipped root and on a trial root. The dead fields are resolved:
`notes` is a declared member of the loader's row that no rule reads, kept deliberately off the `Park` record so it cannot enter `PlayTraceIdentity` and move a stored identity SHA; `nightOnly`, `dayOnly` and the Funfair train's `periodSec` are gone from all twelve files, and FD-11's night block and F4-f's mover row bring back what a park needs.
The park list and its cycle order are data — a required, unique `pickOrder` per park, read into `ContentCatalog.ParkPickOrder`; the six-id literal in `ExhibitionPick` is gone. The home-park map is data: a captain's home park is the park whose `faction` is his, else the default park, and the validator refuses two parks with one faction. An unknown park id is a stop, not a silent Harbor (`ContentCatalog.MustPark`; `cli match --park nope` names it and exits non-zero).
`ExhibitionPick.DefaultPark` is the one `"harbor-diamond"` literal left in `GrandSluggers.Sim` outside the two that F4-a and F8-a own, and a source test holds that (`ParkSchemaTests`, SF-02 / SF-04 sim half). A trial park must still carry every key its shipped park carries (`DataRoot.cs:280-341`, `CompactGeometryTests`), so a new park field lands in twelve files. The resolved table per match arrived next:
✅ — `RulesTable.AtPark(park)` beside `AtLevel`, and with it the first park field this section owns, an optional **`environment`** block (`dragMul`, a multiplier on the root's `flight.drag`, refused at or below 0 and above 4; `windMul`, which replaces `flight.windMul`, refused outside [0, 1]; both optional, absent meaning the global number, and an unknown key inside the block refused by the same strict read).
A park that names none resolves to the global table itself, so Harbor's air is the global air by construction; **no shipped or trial park names one**, so no park file moved and the first number is a trial (F9-a), not a schema change. The block is air only: roll, bounce and the wall carom become ground and wall-material rows with their own libraries (F3-b, F3-c), and `timeScale` can never be per park (D21, one clock).
✅ **F3-b** brought two of those libraries and the park's second optional block, **`zones`** (`infieldDirt`, `outfield`, `warningTrack`, `foulApron`; each optional, each naming a row in `grounds.json`; an unknown key or an unauthored id refused by the same strict read).
A zone the park does not name is **derived from `surface`** — `outfield` and `foulApron` are the surface, `infieldDirt` and `warningTrack` are `dirt` — so `surface` keeps exactly the meaning it has, and `surface` itself is now validated against the library rather than against a set spelled out in the validator. **No shipped or trial park names a `zones` block**, so no park file moved and no stored trace identity did either; the first unequal zone is a trial (Crystal, F9-a).
Which zone a point is in is the geometry the field already had — the `flight.classes.infieldLipFt` lip, `ParkDiamond.TrackWidth` inside `AtBatResolver.FenceAt`, the ±45° chalk — **read, not moved** (the race calibration keeps those numbers), and `GroundZones.ZoneAt(x, z)` is the one function F3-c and F3-d call. The map is resolved from the park beside the table rather than hung on it, the way `ParkBoundary` resolves the edge:
`AtPark` hands back the global table *itself* for a park that names no air (`SF-01`), and a per-park map on that table would have made every park's table a copy. What is still open: a span naming its own wall material (F2-c). ✅ moved the readers onto the rows: the ball and both loose-ball models read the zone under the ball (`GroundZones.RowAt`) and the carom the segment's material (`WallMaterial.OfSegment`), and the flight's and the fielding table's copies are gone.
✅ **F2-c** brought the park's third optional block, **`fence`** (FD-06 C, FD-06-R1, FD-12 B): `points`, from the left-field pole to the right, each `{ bearingDeg, fenceFrac, heightFt, material }` — the bearing in the spray frame, the distance as a fraction of the park's own three-post fence at that bearing (fence-relative, so the one block serves both roots and lands where the migration rule puts it, within the posts' own rounding), the top in feet (unscaled),
and the optional `walls.json` material of the span to the next point. Refused by name on the root the file is in: fewer than two points; a first point off the left line or a last off the right; a bearing not strictly greater than the one before (equal is a fence behind a fence, smaller an overhang, FD-06-R1); a `fenceFrac` not greater than 0; a point or a straight span inside the root's lip; a height missing or not over the root's rail;
a material with no row (`SF-03`) or one on the right-field pole's point; an unknown key in the block or a point. It is carried onto `Park` as the last, defaulted member, so a park that names none writes nothing into the trace identity. **No shipped or trial park names one.** ✅ **F4-d** brought the fourth optional block, **`night`** (FD-11 B, FD-11-R2):
one key today, `hazards`, the instances that exist only at night, in the day block's shape and held to the day block's rules — the type library and its authored row (`SF-03`), the strict read, a disc for anything that acts, and the FD-19 placement rule at the disc the instance plays at night — plus one more:
each must be an instance of a pattern the hazards switch removes (`statusVolume`, `ballRedirect`, `rewardTarget`, `catchStealer`), because night hazard instances are hazards (FD-10-R1); a wall trait or a decoration belongs to the day block. The block has room for look fields, the view outside the stadium (F6-c), and defines none.
Night keeps the stadium lights, so any other key in it — a day park's rule (`windMph`, `environment`), the dropped `nightContactWindowMul`, a look key nobody has defined — is refused by name with that reason, and an empty block is refused. It is carried onto `Park` as the last, defaulted member (`Park.Night`), and only the one resolution reads it (`PlayedPark.Of`); the park a match holds carries none. `nightContactWindowMul` is no longer a park key on either root.
Funfair names one on both roots (its three chompers); no other park does.

Files and the sections each owns (P0 moved the numbers that existed; later epics add fields, not literals):

| File | Sections |
| --- | --- |
| `pitching.json` | See note *pitching.json, Sections* below. |
| `batting.json` | See note *batting.json, Sections* below. |
| `flight.json` | gravity, drag, the shared `timeScale` (§6.1), plate height, `windMul`, sample rate; `classes` (the §6.2 table: topper / grounder / chopper / liner bands, the chopper's hop, the infield lip), `deadBall` (homer trot, foul flight hold). The `carry` hit bands are gone (P4): the bodies decide the bases. `bounce`, `skid`, `roll` and `wall` are gone too: they are the ground rows in `grounds.json` and the wall row in `walls.json`, read off the zone under the ball and the segment it meets, on both roots |
| `grounds.json` | See note *grounds.json, Sections* below. |
| `walls.json` | See note *walls.json, Sections* below. |
| `fielding.json` | See note *fielding.json, Sections* below. |
| `hazards.json` | See note *hazards.json, Sections* below. |
| `running.json` | See note *running.json, Sections* below. |
| `stars.json` | `meterMax`, `gains` (the base `plateAppearance` and the per-event bonuses, the double play and the robbed homer included), `tiers` (the price of each cost tier), `costs` (`guestCaptainSurcharge`), `startingReserve` (both teams' Stars at the first pitch), `mvp` (the §12 point table and the MVP line thresholds) |
| `cpu.json` | See note *cpu.json, Sections* below. |
| `match.json` | `extraInningsCap`, `mercy` (`runs`, `fromInning`, `minScheduledInnings`) — §1 |
| `infield.json` | See note *infield.json, Sections* below. |
| `boundary.json` | See note *boundary.json, Sections* below. |
| `fielders.json` | See note *fielders.json, Sections* below. |
| `body-classes.json` | `classes`: one row per body class, room for fifteen (§8.1) — `id`, `motionStyle`, optional `owedStyle`, `groundReachFt`, `flyReachFt`, `contactWidthMul`, `accelSec`, `brakeSec`, `knockbackMul`. Each row is read as strictly as a nested table: a missing or unknown key is refused by name, an id twice is refused, `knockbackMul` is at most 1. |

Notes to the table above:

**pitching.json, Sections.** `speed` (the Pitch-stat coefficient every family shares), `zone` (the safety net on a batter's thigh-to-chest zone: `bottomMinFt`, `bottomMaxFt`, `topMinFt`, `topMaxFt`, `heightMinFt`, `heightMaxFt`; §4.4), **`families`** (one named row per pitch family, all five authored — `mph`, `chargeMph`, `hump`, `hangUntil`, `hangRate`, `dumpRate`, `dropFt`, `sweepFt`, `sweepFrom`, `breakDamped`, `staminaCost`, `offSpeed`; §4.2, §4.3), `release` (Nice!
band and ×), `flight` (release hand, `AirSeconds` scale and clamps, break cap / ramp / damping / rate), `starShapes` (heat, prism, charm, phony, cask wobble), `stamina` (the pool, the pitch costs every family shares, TIRED threshold, tired aim wobble, the CPU swap lead), `cpu` (the CPU pitcher's table, §4.8, PH-18-R1:
four count rows, each a `location`, a `starChance`, the **`families`** block (`fastball`, `changeup`, `curveball`, `slider`, `sinker` — shares, filtered at run time to what this pitcher can select) and `chargeChance` / `steerChance`, the two independent modifier rolls; the horizontal `locations`; scatter, tap and Nice!
rolls; and `pickoff`). There is no `humanInputs`, no exclusive-verb weight, no `rubberWalkChance`, no `rubberWalkMax` and no `locations.middleYSpreadFt`. The rubber walk distance is geometry (`HomeSet.PitcherWalk`)

**batting.json, Sections.** `window` (**`frames`**, the one window for every hitter, swing and rung (§5.3, PH-10-R1), then floor, square fraction, `leadSec` the square press before the ball's plate time, D13; no `shared` switch, slap / charge frames or per-contact), no stick switch (no `geometryOnly`:
an ordinary swing reads no stick, and bunts and Star Swings read `spray.stickDeg` and `launch.stickDeg`, which is why both stay authored; §5.3, §5.4, §5.9, PH-12, PH-18), `charge` (loft), `quality` (`slap` / `charge` exit columns by zone, energy ×), `exit`, `launch` (loft, height, stick, noise, topper and pop bands), `bunt` (launch, pop height, side lean, and `response`:
exit and spread by contact quality), `spray` (zone spread, stick, timing), `foul` (sour pull past the chalk, until P2), `homer` (launch band), `cursor` (barrel half-axes, perfect and rim fractions, contact scale, charge narrowing), `hbp` (body radius, world feet), `star` (the prismball's spray span), `pitchFactor` (charged pitch vs sour / perfect charge, high-Movement damping), `items` (the item's flight, the peel's radius and life on the grass, the CPU's throw margin,
the slip / daze / hop seconds — §12; no roll), `cpu` (the CPU batter's rolls as shipped; §5.9's tracking table lands in P1 part c; `decideLeadSec`, the commit instant, where it reads the flight as it stands)

**grounds.json, Sections.** The closed **ground library** (FD-05): one named row per ground a park's zone may name — `grass`, `dirt`, `ice`, `ash`, `sand`; a new ground is a row here and an id in the library. Each row carries `roll` (`friction`, `restSpeed`), `bounce` (`restitution`, `horizontal`, `minVy`), the whole `skid` block (its incoming-impact blend band included:
the band is judged against the row under each bounce), `overthrow` (`decelFtPerSec2`, the loose ball's roll) and `bobble` (`restitution`, `groundRetain`, `decelFtPerSec2`, the local bobble on the ground), and **every row is today's number** — what `flight.json` and `fielding.json` carried when F3-c moved them — so the ground under the ball changes no play. Named properties like `pitching.json` `families` (D20):
a `Dictionary` would bypass the reflective range walk, the unknown-key refusal and the JSON = code parity test (FR-02). A ground with no row is a stop that names the id and the file (`SF-03`), in a park's `surface`, in a park's `zones`, or asked for in code. **This is the only copy**:
the batted ball, the overthrow and the local bobble read the row of the zone the ball is in through `GroundZones.RowAt`, and `GroundLibraryTests` pins every row to the shipped numbers written in the test (FD-05). No zone differs yet. The first unequal row is a trial (Crystal, F9-a). ✅:
each row also carries `body` — `startMul` × `fielding.chase.accelSec`, `brakeMul` × `chase.brakeSec`, `cutMul` × the time the across-heading correction takes (the ramp time), `slideMul` × `running.bags.slideFt`, `overrunMul` × `running.bags.overrunFt`. Each scales a time or a length, so a number above 1 always reads "more slippery", and none touches the rated speed, the stick or the heading.
A body reads the row of the zone it stands in, a runner the row of the bag's zone (`GroundZones.RowAt`, §8.1, §9.4). **Every row is 1.0**, so both roots are bit-identical; start, brake and cut-back act only where the response law is on

**walls.json, Sections.** The closed **wall-material library** (FD-06): one named row per material a fence span may name. One row, `padded`, carrying what `flight.wall` carried (0.48 / 0.82) — every span of every park is that wall today, and since F3-c it is the only copy: the carom reads the row of the segment's material (`WallMaterial.OfSegment`, `padded` for every segment until F2-c). No `climbable`, no `robFt`:
a trait belongs to a stretch of wall, not to the stuff it is made of, and spans arrive with the polyline fence (F2-c). A material with no row is a stop that names the id and the file (`SF-03`).

**fielding.json, Sections.** `chase` (the one glove speed, swap lock, the loose-ball scoop reach), `reaction` (the lockout per position, the CPU throw delay), `cover` (flat cover speed and start, D11; the cover radius a throw must land inside; the backup distance), `dash` (chase ×, buddy toss, kick, dive lunge, item smash), `catch` (radius, windows, reaches, ability windows, jump/dive arm times), `drops` (star effects only), `wallPlant`, `abilities` (catch, range, and throw bonuses;
the Lick / Grow tag reach), `throw` (the one throw model: release, base speed, arm, lateral σ, the lob wait, the fly reach, the unassisted step-on distance), `overthrow` (how much of a missed throw's speed carries past the target and how far it may roll;
how fast it slows is the ground row's, F3-c), `catcher` (where the catcher holds the ball behind the plate, the CPU release), `chem` (good speed, the slant), `bobble` (the glove's help to Hands and the fumble's stun), `recoil` (the impact recoil off the ball's speed), `handling` (the awkward hop, the chance curve and the local bobble's spill, stun, ceiling and settle;
the bobble's restitution, ground retain and rolling deceleration are the ground row's since F3-c), `park` (`shellWarpChance` alone — a star swing's flag, not a park hazard's number)

**hazards.json, Sections.** One authored row per hazard type (§14, FD-09, FR-02, FR-08): `freezeVolume`, `lavaPit`, `fireBreath`, `warpPipe`, `barrel`, `billboard`, `climbWall`, `chomper`, `statue`, `train`, `acUnit`, `tree`, `lilyPad`.
Each row names its `pattern` from the closed set (`statusVolume`, `ballRedirect`, `rewardTarget`, `catchStealer`, `wallTrait`, `decoration`) and carries `nightRadiusMul` (a `statusVolume`'s disc at night — 1.6 for `fireBreath`, 1 elsewhere; a type's own night number, which FD-11-R2 keeps) `reachPadFt` (a `ballRedirect`'s reach outside its own radius —
5.6) and, on the three `statusVolume` rows only, `slowSec` (how long a touch slows the body that made it — **3.0** on both roots, Jack's number, FD-08-R2; `[Optional, Positive]`: every volume must author it and no other row may). `nightOnly` left the rows with F4-d: an instance is night-only because its park authors it in its `night` block.
Named properties rather than a dictionary, so the reflective validator and the JSON = code parity test reach every row; the key is derived from the type id (`HazardType.Key`) so the library and the table cannot be spelled apart.
A number its pattern never reads is refused, the way a sweep with no start is. **The numbers moved, they were not chosen**: the race calibration still owns the pad and the multiplier, and `fielding.chase.frozenMul`, `fielding.drops.frozen` and `stars.gains.billboard` stayed where they were because a star swing sets the same slow.

**running.json, Sections.** `bagSec` (the one speed: base, per Run, clamps, dash, the batter's start delay, the no-pass gap), `bags` (occupy radius, tag reach, tag-safe radius, `timeOnBagSec`, the slide, the run-through at first), `close` (the close-play margin, icon delay, CPU reaction), `rundown` (range, the lazy lob), `steal` (the jump, D2:
the air speed fraction, the perfect window and its early break; the CPU catcher's trailing-runner preference; the cut in front of second), `stick`, `dash` (mash per press), `cpu` (the §9.9 thresholds; the §11.6 steal table; the pickoff read's pip multiplier and the corners chance)

**cpu.json, Sections.** `level` (the default rung) and the `easy` / `normal` / `hard` rungs: timing-σ ×, reaction × (CPU batter σ and tracking, close-play reaction, catcher release, the fielder's throw delay), mistrack ×, makeable margin (§8.8), perfect-steal chance and pickoff chance (§11.6, §4.5), `runnerMarginSec` (§9.9). No rung touches a pad's swing window (PH-17; the title names the rung as CPU SKILL; there is no `humanWindowMul`).
Normal is ×1 everywhere so the tables read as written. A match plays at one rung (`Match.Difficulty`, `RulesTable.AtLevel`): the title picks it next to the innings (X / LB), `cli match --difficulty`, every other table shared. ✅ P7

**infield.json, Sections.** `baselineFt`, `moundFt`, `cornerFt`, `secondFt` — the diamond every park shares, read by `Diamond` — plus `innerHalfFt`, `backArcFt`, the ground that dresses it, read by `ParkDiamond`. The corners are authored, not derived: a 90-ft baseline rotated 45° is 63.6396…, and the diamond has always played at a rounded 63.64.
The validator orders the grass vertex inside both the corner bag and the back arc; it deliberately does not require the vertex to clear the 12-ft bag pad, because the pad does not scale and a small enough trial would fail on a dress that draws correctly — the tests carry that margin instead.
Outfield depth and the foul area are **not** here — those vary per park (accepted as D21, FD-07, not built) — and neither is the park environment, which is the park file's own optional `environment` block (FD-03)

**boundary.json, Sections.** `foulOffsetFt` (36), `flareStartFt` (95), `railHeightFt` (4.2), `backstopZFt` (−36), `dugoutPadFt` (18) — where the field ends, read through `ParkBoundary` by the flight's clip polygon (`FieldBounds`), the drawn kit (`HarborWall`) and the park validator's fence floor. These were `HarborWall` constants until F2-a: a class named after one park decided the foul wrap of all six, and the edge could not be changed without editing code (FD-07, FR-05).
One global set for now, like `infield.json`, and a value type rather than five `const`s so that a park may name its own foul area later (F2-d) and the polyline fence (F2-c) has somewhere to land; the polygon cache keys on the resolved edge, so a second edge cannot be served the first one's polygon. **The numbers moved, they were not chosen**: the offset and the flare are still open.
Whether the 80-ft field scales the offset or the flare is an open question. `railHeightFt` is authored as 4.2 and the geometry stands at `(float)4.2`, which is what the `float` constant it replaced always gave (`ParkBoundary.RailTopFt`); widening it moves the polygon and belongs to whoever owns the value

**fielders.json, Sections.** `first`, `second`, `third`, `short`, `left`, `center`, `right` — where each glove stands with nobody on, in feet from home, read by `Diamond.Positions`. `xFt` is signed (the left side of the field is negative), `zFt` is out from home. Seven, not nine: the pitcher stands on `infield.moundFt` and the catcher on `HomeSet.CatcherZ`, and naming either here would be a second source of truth.
One global set for all six parks, outfield included (race decision 3) — per-park depth is new behaviour (accepted as D21, FD-07: the three outfield starts only, not built). A trial does not scale these: the infield four take the basepath scale, and the outfield three keep each body's bearing and its fraction of the fence **at that bearing**, through the circular fence arc (`AtBatResolver.FenceAt`).
LF/RF keep 0.7203 of a 379.16 ft fence, CF keeps 0.7625 of 400 — two different fractions, so no single scale factor describes them

Feel values that were dead or shadowed (`throwEase`, `chargeDecay`, `inPlayCommitSeconds`, `runHz`) are removed from `table.json` and `FeelTable` (✅ P0), and `cpuVsHumanTake` / `cpuVsHumanMiss` with the forced-miss clamp (✅ P1); `fieldAssistStick` is the one stick-take threshold and `FieldAssist` reads it (the duplicate `FieldAssist.StickTake` constant is gone).

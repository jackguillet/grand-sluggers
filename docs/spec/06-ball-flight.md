# 6. Ball flight and the field

## 6.1 Flight

**Ground response:** each impact reads the actual incoming velocity and the row under the ball. Impact angle is `atan2(downward speed, horizontal speed)`. At or below `skid.impactMinDeg` (14°), use that surface's skid response; at or above `skid.impactMaxDeg` (22°, ice 28°), use its bounce response. Between those angles, interpolate restitution, horizontal retention and settling speed continuously.
Re-evaluate at every bounce and after a deflection; original launch angle and hit class have no role. Grass/dirt retain skid restitution 0.28 and bounce restitution 0.48, horizontal retention 0.93/0.82 and vertical settling speed 2.2/3.6 ft/s. Early and late hops are the result of contact and impact, with no authored bounce count.

- 3-D ballistic with drag taken relative to a directional park wind (`Park.WindMph` + `WindDeg`, `flight.windMul` of the flag reading), integrated at 120 Hz. ✅ P2 (`BallFlight.Trajectory(exit, launch, spray, park)`; every `Sample` carries X / Z / height and an event).
- **Drag and wind are read off the match's resolved table** (§0.3, FD-03), so a park may name its own air: the optional `environment` block carries `dragMul`, a multiplier on the root's `flight.drag` (so a trial root's drag stays the trial's), and `windMul`, which replaces the global exposure. Absent is the global number, and no shipped or trial park names either — every park flies Harbor's air today.
- **A park's wind may turn each inning** (the optional `environment.windSchedule`: `minMph`, `maxMph`, `nightMul`). Before each inning's first pitch the match draws the inning's speed across the band and its bearing (any of 360°) from its own wind stream, and at night the speed is `nightMul` times as strong. The wind never changes inside an inning, let alone a play.
  A park without a schedule draws nothing and keeps its fixed `windMph` / `windDeg`, bit for bit. The field card names the gusts, and the line under the scorebug reads the inning's wind in the field's words (`CarnivalFront.WindLine`). Summit Park: 3–14 mph, ×1.4 at night (`WindScheduleTests`).
  Gravity, the shared stretch and the plate height are global in every park (D21), and the ground and the wall are not in this block (F3-b, F3-c). ✅ (`RulesTable.AtPark` → `FlightRules.WithEnvironment`; `ParkEnvironmentTests.SF10_ThickerAirIsAShorterCarryOffTheSameLaunch`, `…SF10_AParkAtWindMulZeroFliesTheStillAirPath`). ✅ the reach of that table is whole:
  `Match` builds `AtBatResolver` and `FieldingResolver` on `_rules` and passes it to every call it makes, so the at-bat's own flight and the fielding preview — the path the live ball is normally built from — fly the park's air the same way the live ball's continuation after a deflection already did, and `Content.Rules` is read in `Match` at exactly one place, the derivation itself. `LivePlaySystem` already read `_match.Rules` throughout.
  A seeded swing run through `Match` / `LivePlaySystem` at a fixture park with `dragMul` 2.0 carries shorter in all three (`ParkEnvironmentTests.SF10_ASeededSwingThroughTheMatchFliesTheParksAir`), and every resolver holds the match's table by reference on both roots, at every park and every rung (`…SF01_EveryResolverInTheMatchHoldsTheMatchsResolvedTable`). The rung travelled with it:
  the fielding preview's CPU reaction lockouts (`Cpu.Active.reactionMul`) are now read at the match's difficulty, the one `LivePlaySystem` already waited, so an EASY or HARD game picks its pursuit on the rung it is playing. NORMAL, and therefore every stored number, is unmoved.
- Sample times are stretched by `TimeScale` (1.65) so gloves can get under a fly. Carry does not change. Pitches and throws are **not** stretched. **The stretched sample clock is the play clock**: `LivePlaySystem.ElapsedSeconds` advances on it, the ball sits at `PointAt(path, ElapsedSeconds)`, and every glove, runner, and throw is judged against it — one clock (§0.3). ✅ P2 (explicit in `BallFlight` and `FlightRules.TimeScale`; the one-clock test in `FlightScenarioTests`).
- **One flight clock:** every batted ball uses `flight.timeScale` **1.65**, independent of launch, exit speed or descriptive class. Gravity and velocity produce the different hang times. Neither `linerTimeScale` nor `dirtTimeScale` is a supported rule key. Throws and pitches keep their own clocks.
- Bounce: restitution 0.48, horizontal 0.82; shallow impacts skid (0.28 / 0.93), blended by incoming angle as above. Roll friction 22 ft/s². Rest at 1.4 ft/s. Wall carom: normal × 0.48, along the wall × 0.82. ✅ Every one of these is a ground row or the wall row since F3-c: `data/rules/grounds.json` (the same numbers in `grass`, `dirt`, `ice` and `ash`) and `walls.json` `padded`, read off the zone under the ball and the segment it meets (next bullet); `flight.json` no longer carries them.
- **The ground under the ball (FD-05).** ✅ built the two closed libraries the numbers above will move into — `grounds.json` (`grass`, `dirt`, `ice`, `ash`, `sand`) and `walls.json` (`padded`) — and the zone map that says which row a point is standing on:
  a park's ground is `infieldDirt`, `outfield`, `warningTrack` and `foulApron`, taken from the shared diamond's own geometry (the lip, the track, the chalk), each naming a row, each derived from the park's `surface` unless the park's optional `zones` block overrides it. **Every row is today's number and no park overrides a zone**, so the ball rolls, hops and caroms exactly as it did. ✅ ** moved the reads.**
  Every roll step of the batted ball reads `roll` (`friction`, `restSpeed`) from the row of the zone the ball is in at the start of that step, and every bounce reads `bounce` — or `skid`, when the ball's launch is inside **that row's** band (the launch is the ball's, the band is the ground's) — from the row of the zone at the bounce point; the one lookup is `GroundZones.RowAt(x, z, grounds)` on the match's table (FR-01).
  A carom reads the row of the segment's wall material, and `WallMaterial.OfSegment` is the one place that answers "which material" — `padded` for every segment of every park until the polyline fence (F2-c) gives a span its own. The open field (`BallFlight.Trajectory(exit, launch, wind)`, no park and so no zones) names its ground, `BallFlight.OpenFieldGround` = `grass`. The two loose-ball models the live play runs read the same row, each tick, at the ball's place:
  the overthrow (a sailed throw, a lob dropped at an uncovered bag, the shipped fumble's scatter — `BallFlight.OverthrowTick`) slows at the row's `overthrow.decelFtPerSec2`, and the local bobble (`BallFlight.LocalBobbleTick`) rebounds at the row's `bobble.restitution`, keeps `bobble.groundRetain` of its roll per impact and slows at `bobble.decelFtPerSec2`; the third ground model, the deflected ball, was already the batted ball (`BallFlight.Continue`).
  `flight.roll` / `bounce` / `skid` / `wall`, `fielding.overthrow.decelFtPerSec2` and `fielding.handling.bobbleRestitution` / `bobbleGroundRetain` / `bobbleDecelFtPerSec2` are gone from both roots; the throw's `carryMul` / `maxRollFt` and the fumble's spill, stun, ceiling and settle stay the throw's and the fumble's. A zone that names a ground with no row stops the ball the moment it needs that row — it never rolls on grass by default (FR-02).
  With equal rows every path and every loose-ball tick is the pre-move one to the bit on both roots, held against the pre-move code (`GroundReadTests.EqualRows…`), and seed 7 and the park-factors table did not move. The body's grip on a ground is F3-d's, and a span's material is F2-c's.
- **Fence**: the path is clipped by the park boundary (`FieldBounds.Of(park)`): the outfield fence between the poles is the circle through the park's L / C / R posts (`RoundFence`) at `fenceHeightFt`; the foul wraps are the diamond kit's hip rail (36 ft off each line, flaring to the pole) and the round backstop (36 ft behind the plate). The drawn wall is the same number (D15, `HarborWall.OutfieldHeight(park)`, `HarborWallTests`). Below fence height → wall carom (§7.9).
  Above, between the poles → home run. Bounce then over → ground-rule double. Any touch of a foul wall, or over one, → foul, dead. ✅ ⚠️ Fields audit: that last sentence holds only for a ball not yet decided. A ball already fair caroms off the rail and stays live, and a fair ball that then clears the rail is a ground-rule double (`BattedBall.cs:131-170`); the code is the intended rule and this sentence is too coarse.
  The drawn rail ramps from 4.2 ft to the fence from 95 ft out while the sim rail stays 4.2 ft to the pole (`HarborWall.cs:177-191`, `FieldBounds.cs:159`), and the drawn wall loop mirrors right field onto left (`HarborWall.cs:99-101`), so a lopsided park draws a wall the ball does not meet. ✅:
  the foul wrap, rail, flare, backstop and dugout pad are no longer Harbor's literals — they are `boundary.json` (§16) read through a `ParkBoundary` value type, `FieldBounds` and the park validator's fence floor name no park's class, and the polygon cache keys on the whole edge so a per-park foul area cannot be served a stale polygon. Every value is the one that shipped and the polygon is unchanged vertex for vertex (`SF-08`); the flare and the offset are still open to choose.
  ✅: **the mirror is gone**. The drawn loop walks the whole boundary — each side's own fence through `FenceAt`, that side's foul wrap and the backstop — on the spray grid `FieldBounds` samples, so every drawn vertex lies on the clip polygon (worst 5e-15 ft) and a lopsided park draws its own left field (`SF-05`). The three symmetric parks draw bit-identical loops, so nothing changed on screen; the drawn-loop cache now keys on the whole edge, as the polygon cache does.
  ✅: **the ramp is gone.** §5 **Q5** was answered (Jack, FD-06-R2): the sim's rail is the rule. Each drawn span now stands at the top of the flight segment it lies on, read at each of its two ends (`HarborWall.FlightSpan` / `SpanTops`, read from `FieldBounds.Of(park)`, not recomputed), and a drawn vertex at the tallest segment that meets there (`HarborWall.Height`).
  So the drawn rail stays hip-high to each pole, and the wall steps from the fence to the rail at the pole vertex, where the flight's segment kind changes. The kit draws each span from its own tops (`FieldKit.Wall`), so that step is vertical at the pole and its screen; a span is level wherever its two ends match — every span of every park today — and sloped only along a polyline span whose two points differ.
  Before, from 95 ft out (`RampStartZ`) the drawn rail rose to the fence by a smoothstep, and the last 31–34 ft of rail before each pole (11–12 ft on the compact field) was drawn at full fence height, because "outfield" meant within half a degree of the line. No flight, boundary or rule number moved. ⚠️ Look gate at review (Jack): the foul poles at Harbor and the five parks. ✅**: the polyline fence** (FD-06 C, FD-06-R1, FD-12 B).
  A park may name `fence.points`, from the left-field pole to the right-field pole; each point is a `bearingDeg`, a `fenceFrac` (its distance as a fraction of the park's own three-post fence at that bearing, so one authoring serves both roots) and a `heightFt`, and each span names a `walls.json` material (default `padded`). The wall is straight between two points and its top runs straight between their heights.
  The validator keeps it one distance per bearing (first point on the left line, last on the right, every bearing strictly greater than the one before) and inside the park (past the root's lip, over its rail, a material with a row), by name on both roots. `AtBatResolver.FenceAt` is still the one fence function:
  for a park with points it is the distance at which the ray meets the one span at that bearing, else the three-post circle, bit for bit; `FenceSpotAt` answers the top and the span's material from the same resolution.
  The clip polygon is built on the spray grid with every point merged in, so every point is a vertex and no corner is cut; each piece carries its top at both ends (`WallSegment.HeightAt`) and its span's material, and the flight compares the ball with the top **where it crossed** (`FieldBounds.Crossing.HeightFt`) — the same number for every level piece, so a park without points flies to the bit as before.
  The polygon and the drawn loop are cached on one key that carries the fence by value (`FieldBounds.EdgeKey`, map finding 14). The track, the poles, the zone map and the drawn loop read `FenceAt` and follow; the drawn loop walks the same bearings and draws each span at the flight's top. **No shipped or trial park names points**:
  seed 7 and the park-factors table day and night are identical on both roots, and the polygon, `FenceAt` and the drawn loop of every park are the pre-polyline ones to the bit (`PolylineFenceTests.SF06_…`). ⚠️ The rob clearance still measures against `fenceHeightFt` (§7.9).
- **Foul lines / backstop / side walls** are the field boundary; the ball cannot leave the park except over the fence or into foul stands (dead). ✅ P2 (property test: no sample of a fair trajectory lies outside the polygon except above the fence).
- **Landing mark**: the first ground contact of the clipped path; the ring predicts that point while the ball is unbounced, and ground-ball descriptions suppress it. An ordinary catch meets the live ball within reach and height. Wall plant if the ball meets the wall or clears the fence first (`BattedBall.HangT` is that instant; the catch window sits on it). ✅ (`FlyCatch.ChaseTarget`)
- **Ball ground shadow**: a dark translucent disk follows the live ball's current X/Z through flight, bounces and throws; held and hidden balls have no disk. It clears the field skins and follows the mound. Both seats and every captain use the same presentation, including HUD-off. The yellow/red landing ring remains the predicted catch/landing cue.
  In `data/feel/table.json`, `ballShadow` starts at **5 ft diameter** at ground level, shrinks linearly to **3 ft at 60 ft high**, and stays at least 3 ft; **0.65 opacity**, **0.04 ft lift** above the highest flat skin or mound. These are original readability values, not measured Sluggers dimensions. ⚠️ Jack's standalone visibility gate remains open. Reference:
  [Sluggers fielding guide](https://gamefaqs.gamespot.com/wii/945802-mario-super-sluggers/faqs/53987) describes following the ball's shadow.
- **Ground contact is geometric:** the ball bounces as soon as its integrated height reaches the surface, including an early downward hit. No minimum flight-time guard delays the first bounce. Hang, carry and catch eligibility read the first surface-contact event from the same path.
- **Velocity units:** path continuations accept velocity measured in play seconds and convert it once for integration. Hop difficulty uses physical vertical velocity. Redirects and deflections preserve the requested speed fraction without an extra slowdown.

## 6.2 Batted-ball classes

These are descriptive reads for tactics, cameras and copy, not physical modes. Every class shares the clock, surface response and catch geometry. A ball described as a grounder can still be caught if a glove meets it before the first surface contact; any ball already bounced is a pickup. Class boundaries never change flight speed or restitution.

Class is a function of launch angle and exit velocity at contact, used by fielding assignment, cameras, and runner AI. One table (`data/rules/flight.json`):

| Class | Launch | Exit | Typical |
| --- | --- | --- | --- |
| Topper | < 3° | any | Weak roller in front of the plate |
| Grounder | 3–10° (and 10–14° under 74 mph) | any | Infield hop |
| Chopper | < 14° with a first bounce inside 30 ft and bounce height > 3 ft | ≥ 70 | High bounce, slow to the glove |
| Liner | 10–22° | ≥ 74 (`linerMinExitMph`; P7 lowered it from 78) | Low direct flight; shared clock (§6.1) |
| Fly (infield / pop) | > 22° (or 14–22° under 74 mph) | first landing < 137.78 ft (`classes.infieldLipFt`) | Pop-up, long hang |
| Fly (outfield) | > 22° (or 14–22° under 74 mph) | landing ≥ 137.78 ft | Routine / deep by carry |
| Wall ball | fly or liner that meets the fence below fence height | | Carom |
| Home run | crosses the fence above height | | Dead, runners circle |
| Bunt | bunt verb | | Dribbler in the triangle |
| Foul | lands / touched in foul territory | | Dead unless caught |

✅ P2: `BattedBallClass` and `BattedBall.Of` (`flight.classes`). `BattedBallClasses.ByLaunch` is the read at the crack (topper / grounder / liner / fly) the cameras use; the flight refines it (chopper by the first hop, pop by the landing, wall and homer by the fence). `AtBatResult.Class` and `FieldingPreview.Class` carry the shape (topper … homer, bunt) with `Foul` beside it as the chalk; `BattedBall.Class` is the spec's row (Foul when the untouched path is foul).
A bunt popped up (§5.8) is a pop, not a bunt. The pursuit pool is `FieldingResolver.PursuitPool(class, foul)`. The chopper row reads "< 14°" rather than "3–14°": launched from the bat's height, no ball at 3° and 70 mph first bounces inside 30 ft — the chopper is a ball driven down, and it becomes reachable once §5.4's launch bands (P1) include the topper band below 3°.
